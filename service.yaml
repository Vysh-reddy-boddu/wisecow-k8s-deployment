apiVersion: v1
kind: Service
metadata:
  name: wisecow-service
spec:
  selector:
    app: wisecow
  type: NodePort
  ports:
    - protocol: TCP
      port: 4499       # service port
      targetPort: 4499 # container port
      nodePort: 30001  # external access in Minikube/Kind

